   services:
     - type: web
       name: cipher-threat-api
       env: docker
       plan: starter
       autoDeploy: false
       dockerfilePath: Artifacts/Dockerfile
       healthCheckPath: /health
       numInstances: 1
       envVars:
         - key: PORT
           value: "8000"
